# 詳細照合 作業台帳

> 作成日: 2026-06-01
> 対象: ELE-CAL 各計算ツール
> 前提: MDは検索補助。確定値は原本PDF目視で確認する。
> 参照ページの過不足確認: `docs/根拠/参照ページ索引.md` を優先する。

## 1. 優先順位

| 優先 | ツール | 理由 | 状態 |
|---:|---|---|---|
| 1 | `voltage_drop` | 単相3線式係数 `35.6` vs `17.8` の疑義が計算結果に直結 | PDF目視済み / 実装要件確認待ち |
| 2 | `pipe_size` | 補正係数と管内断面積表の誤りは推奨管サイズに直結 | PDF目視済み / 実装修正候補あり |
| 3 | `apartment_main` | TODOのページ表記ズレがあり、表データ欠落疑義もある | PDF目視済み / 実装データ要修正 |
| 4 | `breaker` | 2026-05-29完了扱いだったが、PDF目視で電動機表に複数の転記ずれを再確認 | PDF目視済み / 実装修正候補あり |
| 5 | `allowable_current` | 一度修正済みだが、根拠MD本文と `breaker` 側コピーの再同期が必要 | PDF目視済み / 実装データ要再同期 |
| 6 | `cable_rack` | 公共建築参照範囲と独自係数、ケーブル外径・質量根拠の整理が必要 | 外径はPDF目視済み / 質量は別根拠必要 |

## 2. voltage_drop

| 照合項目 | 原本ページ | 根拠MD | 実装 | 確認内容 |
|---|---|---|---|---|
| 電圧降下許容限度 | PDF p120 | `根拠資料/内線規程/md化/需要設備編/03_part1_chapter3_safety_principles_p111-p236.md` | `src/app/voltage_drop_v2/page.tsx` `STD` | 1310-1表の距離区分と許容率 |
| 簡易式係数 | PDF p875 | `根拠資料/内線規程/md化/需要設備編/17_reference_materials_p833-p1054.md` | `src/app/voltage_drop_v2/page.tsx` `COEFF` | 単相2線、単相3線、三相3線の係数 |
| R/Xインピーダンス | PDF p876以降 | 同上 | `src/app/voltage_drop/page.tsx`, `src/app/breaker/constants.ts` | 旧版・管理画面・breaker側の複製値 |

要注意:
- PDF p875上、単相3線式・三相4線式の簡易式は `17.8`（対地間）で確定。
- `src/app/voltage_drop_v2/page.tsx` の `COEFF['1phase3wire'] = 35.6` は、100V対地負荷ではPDF根拠と一致しない。200V線間負荷を単相2線相当で扱うなら `35.6` の余地があるため、入力要件を分ける必要がある。
- PDF p120の1310-1表は `STD` と一致。
- PDF p876の50Hz CVT列は、旧版 `/voltage_drop` と `breaker/constants.ts` の `14-325mm2` の50Hz R/X値と一致。`8mm2` と60Hz値は別資料確認が必要。
- `src/app/voltage_drop/page.tsx` と `src/app/admin/voltage_drop_calc/page.tsx` にR/X方式の複製がある。

## 3. pipe_size

| 照合項目 | 原本ページ | 根拠MD | 実装 | 確認内容 |
|---|---|---|---|---|
| 占有率ルール | PDF p354, p368-p369 | `根拠資料/内線規程/md化/需要設備編/09_part3_chapter1_low_voltage_wiring_methods_p321-p444.md` | `src/app/pipe_size/page.tsx` | 32% / 48% の適用条件 |
| 電線断面積 | PDF p354 | 同上 | `WIRE_AREA` | IV電線の断面積 |
| 補正係数 | PDF p355 | 同上 | `IV_CORR20`, `IV_CORR12` | `1.25 / 2 / 3.5mm2` の扱い |
| 電線管内断面積 | PDF p355, p367-p371 | 同上 | `PIPE_RANGE` | CP/EP/GP/VE/PF/CD の32%・48%表 |

要注意:
- PDF p355の3110-8表では、より線 `1.25`, `2`, `3.5` は補正係数表に記載なし。現行の `IV_CORR20` 2.0扱いは別根拠または設計判断が必要。
- IV単線 `2.6mm = 20`, `3.2mm = 28` と金属管の表割当は2026-06-07に修正済み。
- `GP / PE（ねじなし・PF管相当）` は異なる管種を混在させるラベルのため整理が必要。
- `VE` と `PF-D` に補正係数が適用されていない可能性がある。PDF p370の3115-8表ではVE・PF/CDいずれも補正係数表がある。
- PDF p370-p371は元MD上で表崩れが大きかったが、PDF目視で主要表は確認済み。`pipe_size.md` のVE/PF-CD表値はPDF整数値へ修正済み。

## 4. apartment_main

TODOのページ表記は修正が必要。

| 照合項目 | 冊子ページ | 対応PDFページ | 根拠MD | 実装 | 確認内容 |
|---|---:|---:|---|---|---|
| 3605-1 負荷の想定 | p681-p682 | PDF p763-p764 | `根拠資料/内線規程/md化/需要設備編/14_part3_chapter6_lighting_household_wiring_design_p759-p782.md` | `src/app/apartment_main/calculations.ts` | 標準負荷、加算VA |
| 3605-2/3 分岐回路 | p683-p686 | PDF p765-p768 | 同上 | `src/app/apartment_main/validation.ts` | 分岐回路種別、回路数 |
| 資料3-6-1 一般集合住宅 | p921-p922 | PDF p1003-p1004 | `根拠資料/内線規程/md化/需要設備編/17_reference_materials_p833-p1054.md` | `src/app/apartment_main/data.ts` `GENERAL_TABLE` | 一般集合住宅1-40戸 |
| 資料3-6-2 全電化 | p923-p926 | PDF p1005-p1008 | 同上 | `ELECTRIC_23H_TABLE`, `ELECTRIC_MICOM_TABLE` | 全電化表、17戸以上、ヒートポンプ式給湯機 |

要注意:
- PDF p681-p686は水中照明の内容であり、集合住宅ではない。
- PDF p921-p926はSPD/遮断容量の内容であり、集合住宅ではない。
- 集合住宅の正しい対応は冊子ページ `p681-p686`, `p921-p926` からPDFページへ換算する。
- `GENERAL_TABLE`、`ELECTRIC_23H_TABLE`、`ELECTRIC_MICOM_TABLE` の現行実装範囲は2026-06-07にPDF p1004/p1006/p1007正本値へ修正済み。
- 全電化17〜25戸は遮断器・CVT欄が空欄のため、遮断器・CVTまで表示する現行ツールの16戸上限は妥当。
- PDF p1008のヒートポンプ式給湯機表は現行実装・根拠MDの表データに未収録。実装対象に含めるか要件判断が必要。

## 5. breaker

| 照合項目 | 原本ページ | 実装 | 確認内容 |
|---|---|---|---|
| 200V電動機1台 | PDF p796-p797 | `src/app/breaker/constants.ts` `MOTOR_TABLE_SINGLE` | こう長、分岐回路遮断器、接地線 |
| 400V電動機1台 | PDF p798 | 実装未使用または未整理 | 400V表の転記候補 |
| 200V電動機数台の幹線 | PDF p800-p801 | `MOTOR_TABLE_TRUNK`, `MOTOR_BREAKER_TRUNK` | 幹線電線太さ、遮断器容量マトリクス |
| 溶接機 | PDF p567 | `WELDER_TABLE` | 主要値の一致確認 |
| 需要率 | PDF p779 | `DEMAND_FACTOR` | 需要率、対象建物、計算式 |

要注意:
- `MOTOR_TABLE_SINGLE` は2026-06-05にPDF p796-p797正本値へ修正済み。
- `MOTOR_TABLE_TRUNK.maxCurrent` は一致。12kW・50A行と30kW・125A行の電線管/VVに旧値が残る。根拠MD本文は正本値へ修正済み。
- `MOTOR_BREAKER_TRUNK` はPDF p801の2軸マトリクスを保持せず、現在の計算処理からも未使用。表又は計算式を採用する前に、始動方式を含むp801全セルとの一致試験が必要。
- 計算式案を使う場合、`I_trunk` は `MOTOR_TABLE_TRUNK.maxCurrent` の区分上限値ではなく、入力された各電動機の定格電流合計として検証する。電技解釈第148条の幹線上限とp801の実用最小値は別条件として扱う。
- 旧 `docs/根拠/照合レポート.md` の「修正済み」「完全一致」表現は、今回のPDF目視結果より古い作業記録として扱う。

## 6. allowable_current

| 照合項目 | 原本ページ | 実装 | 確認内容 |
|---|---|---|---|
| IV/HIV許容電流 | PDF p146-p151 | `src/app/allowable_current/page.tsx` | 気中、管内3本以下、管内4本、HIV補正係数1.22 |
| VVF/VVR許容電流 | PDF p880 | 同上 | VVケーブル2心・3心、施設条件別許容電流 |
| CV/CVD/CVT | PDF p881-p882 | 同上 | CV表2、単心2個より/3個より表3 |
| breaker側コピー | 同上 | `src/app/breaker/constants.ts` `ALLOWABLE_CURRENT` | 別コピーの同期 |

要注意:
- PDF p148の1340-2表では、実装のIV「管内3本以下」「管内4本」に+1A程度の丸め差が複数ある。
- CV気中暗渠・直埋、CVD/CVTと `breaker/constants.ts` 側コピーは2026-06-07に正本値へ修正済み。
- CV管路引入れの2〜60mm²はPDF p881単心CV欄が「—」のため根拠未確定。CVD/CVTのUIラベルも表3の「単心2個より / 単心3個より」へ変更が必要。
- HIVはPDF p146/p150/p151、VVF/VVRはPDF p880で主要根拠を確認済み。現行HIV値とVVF/VVRの採用列を再照合する。VVF「埋込配線」は引き続き根拠未確認。

## 7. cable_rack

| 照合項目 | 原本 | 実装 | 確認内容 |
|---|---|---|---|
| ケーブルラック施工基準 | 公共建築標準仕様書 PDF p76-p78 | `src/app/cable_rack/page.tsx` | 支持間隔、ボルト径、固定、曲げ半径 |
| ケーブル外径 | 内線規程 資料0-1 PDF p844-p852 / p856-p859 | `CABLE_TABLE.od` | VVF/VVR/CV/CVT/CE/Fの値を線種ごとに再照合 |
| ケーブル質量 | メーカー代表値 | `CABLE_TABLE.mass` | 内線規程構造表に掲載なし。値ごとの別根拠分類 |
| ラック幅式 | ELE-CAL独自 | 幅計算ロジック | 独自係数として明記 |

要注意:
- 公共建築資料は現行実装では `/cable_rack` のみ直接参照。標準図PDFは現行ラック計算では未使用。
- 公共建築標準仕様書PDF p76-p78の施工条件は根拠化済み。ラック幅式、余裕係数、将来増設率はELE-CAL独自。
- 内線規程PDF p844-p852にVVF/VVR/CV/CVT外径、p856-p859にCE/F外径がある。現行 `CABLE_TABLE.od` は同一線種の原本表と再照合する。
- `CABLE_TABLE.mass` は今回確認した内線規程構造表では未確認。メーカー資料等の別根拠が必要。
- ボンディング注意は公共建築標準仕様書上カバー有無に限定されないため、現行 `hasCover` 条件は見直し候補。
